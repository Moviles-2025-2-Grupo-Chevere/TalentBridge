import 'package:flutter/material.dart';

class LeaderFeed extends StatelessWidget {
  const LeaderFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
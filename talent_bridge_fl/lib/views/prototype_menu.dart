import 'package:flutter/material.dart';

class PrototypeMenu extends StatelessWidget {
const PrototypeMenu({ super.key });

  @override
  Widget build(BuildContext context){
    return Scaffold();
  }
}
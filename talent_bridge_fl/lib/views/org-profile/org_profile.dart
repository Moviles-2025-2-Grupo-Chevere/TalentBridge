import 'package:flutter/material.dart';

class OrgProfile extends StatelessWidget {
const OrgProfile({ super.key });

  @override
  Widget build(BuildContext context){
    return Text('Org profile view');
  }
}
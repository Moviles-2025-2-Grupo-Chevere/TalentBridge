package com.example.talent_bridge_kt.data.repository

import android.content.Context

// Deprecated: This repository is no longer used.
// Offline application queue functionality was removed.
// Applications now rely on Firestore only.
class ApplicationsLocalRepository(context: Context) {
    // Empty stub to avoid compilation errors if referenced elsewhere
}
